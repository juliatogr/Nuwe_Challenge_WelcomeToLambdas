Mediante una interfaz puedo definir las funciones lambda que se piden para el reto (isOdd, isPrime y isPalindrome).

A partir de aquí, puedo definir el método checker que recibe la lista de enteros y llama a las funciones para ir 
obteniendo los resultados correspondientes. Esto se devuelve en una lista de una lista de strings para que se puedan
separar los resultados por cada uno de los números.

La clase solución se encuentra en el fichero MyLambda.java